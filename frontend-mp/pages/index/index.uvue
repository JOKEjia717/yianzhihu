<template>
	<view class="container">
		<!-- 顶部装饰条 -->
		<view class="top-bar"></view>
	
		<!-- 标题 -->
		<view class="header">
			<text class="title">颐安智护</text>
			<text class="subtitle">请选择您的身份</text>
		</view>

		<!-- 登录选项 -->
		<view class="login-options">
			<!-- 院长登录 -->
			<view class="option-card" @click="goToDeanLogin">
				<view class="card-content">
					<text class="option-title">院长登录</text>
					<text class="option-subtitle">Dean Login</text>
					<view class="sub-options">
						<text>○ 系统管理</text>
						<text>○ 人员管理</text>
					</view>
				</view>
			</view>

			<!-- 子女登录 -->
			<view class="option-card" @click="goToChildrenLogin">
				<view class="card-content">
					<text class="option-title">子女登录</text>
					<text class="option-subtitle">Children Login</text>
					<view class="sub-options">
						<text>○ 探视预约</text>
						<text>○ 实时沟通</text>
					</view>
				</view>
			</view>

			<!-- 护工登录 -->
			<view class="option-card" @click="goToNurseLogin">
				<view class="card-content">
					<text class="option-title">护工登录</text>
					<text class="option-subtitle">Nurse Login</text>
					<view class="sub-options">
						<text>○ 日常护理</text>
						<text>○ 健康记录</text>
					</view>
				</view>
			</view>
		</view>

		<!-- 右侧滚动条 -->
		<view class="scroll-bar"></view>
	</view>
</template>

<script lang="uts">
	export default {
		methods: {
			goToDeanLogin() {
				uni.navigateTo({
					url: '/pages/dean/login'
				})
			},
			goToChildrenLogin() {
				uni.navigateTo({
					url: '/pages/login/login'
				})
			},
			goToNurseLogin() {
				uni.navigateTo({
					url: '/pages/nurse/login'
				})
			}
		}
	}
</script>

<style>
	.container {
		min-height: 100vh;
		background: linear-gradient(180deg, #EBF4F5 0%, #B5C6E0 100%);
		padding: 40rpx 30rpx;
		position: relative;
	}

	.header {
		text-align: center;
		margin: 40rpx 0 80rpx;
	}

	.title {
		font-size: 48rpx;
		color: #2C3E50;
		font-weight: bold;
		margin-bottom: 20rpx;
		display: block;
		letter-spacing: 2rpx;
		text-shadow: 0 2rpx 4rpx rgba(255, 255, 255, 0.5);
	}

	.subtitle {
		font-size: 32rpx;
		color: #34495E;
		display: block;
		letter-spacing: 4rpx;
		text-shadow: 0 2rpx 4rpx rgba(255, 255, 255, 0.5);
	}

	.login-options {
		display: flex;
		flex-direction: column;
		gap: 30rpx;
		padding: 0 20rpx;
	}

	.option-card {
		height: 240rpx;
		border-radius: 20rpx;
		position: relative;
		overflow: hidden;
		cursor: pointer;
		transition: all 0.3s ease;
		box-shadow: 0 8rpx 25rpx rgba(0, 0, 0, 0.1);
	}

	/* 院长登录卡片样式 */
	.option-card:nth-child(1) {
		background: url('/static/images/dean-card-bg.jpg') no-repeat center;
		background-size: cover;
	}

	/* 子女登录卡片样式 */
	.option-card:nth-child(2) {
		background: url('/static/images/children-card-bg.jpg') no-repeat center;
		background-size: cover;
	}

	/* 护工登录卡片样式 */
	.option-card:nth-child(3) {
		background: url('/static/images/nurse-card-bg.jpg') no-repeat center;
		background-size: cover;
	}

	.option-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(
			to bottom,
			rgba(0, 0, 0, 0.2),
			rgba(0, 0, 0, 0.5)
		);
		z-index: 1;
	}

	.card-content {
		position: relative;
		z-index: 2;
		padding: 40rpx;
	}

	.option-title {
		font-size: 40rpx;
		color: #FFFFFF;
		font-weight: 600;
		margin-bottom: 8rpx;
		text-shadow: 0 2rpx 4rpx rgba(0, 0, 0, 0.3);
	}

	.option-subtitle {
		font-size: 24rpx;
		color: rgba(255, 255, 255, 0.9);
		margin-bottom: 20rpx;
		display: block;
		font-family: Arial, sans-serif;
	}

	.sub-options {
		display: flex;
		flex-direction: column;
		gap: 8rpx;
	}

	.sub-options text {
		color: rgba(255, 255, 255, 0.9);
		font-size: 24rpx;
		text-shadow: 0 1rpx 2rpx rgba(0, 0, 0, 0.3);
	}

	.option-card:hover {
		transform: translateY(-6rpx);
		box-shadow: 0 12rpx 30rpx rgba(0, 0, 0, 0.2);
	}

	.option-card:active {
		transform: translateY(2rpx);
		box-shadow: 0 6rpx 15rpx rgba(0, 0, 0, 0.1);
	}

	/* 返回按钮样式 */
	.back-button {
		position: absolute;
		top: 40rpx;
		left: 30rpx;
		width: 60rpx;
		height: 60rpx;
		color: #666;
	}

	/* 滚动条样式 */
	.scroll-bar {
		position: fixed;
		right: 10rpx;
		top: 50%;
		transform: translateY(-50%);
		width: 6rpx;
		height: 200rpx;
		background: rgba(0, 0, 0, 0.1);
		border-radius: 3rpx;
	}

	/* 顶部装饰条 */
	.top-bar {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		height: 8rpx;
		background: #4FD1C5;
	}
</style>
